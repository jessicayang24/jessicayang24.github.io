# Policy Atlas

一个适合部署到 GitHub Pages 的静态导航站，收录国务院级、部委级和地方级政策站点，并支持搜索和分类筛选。

## 本地结构

- `index.html`: 页面结构
- `styles.css`: 样式
- `script.js`: 前端交互与数据渲染
- `data/policy_sites_data.json`: 导航数据

## 发布

这个仓库设计为直接发布到 GitHub Pages 根目录，默认分支为 `main`。
